generator client {
  
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url =env("DATABASE_url")
}

model user {
  id int @id @default(autoincrement())
  name string
  email string @unique
}